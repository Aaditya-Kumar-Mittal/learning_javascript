//Javascript by default adds a property called [[prototype]] to every object.
//[[prototype]] contains many helper properties and methods which we can use to complete our task
//Many properties and methods are already available in JavaScript inside prototype of every object, just like length property in arrays.